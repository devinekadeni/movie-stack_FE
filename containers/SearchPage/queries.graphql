query SearchMovies($page: Int!, $searchParams: searchParams) {
  searchMovies(page: $page, searchParams: $searchParams) {
    currentPage
    hasMore
    movies {
      id
      title
      poster
      genreIds
      rating
    }
  }
}

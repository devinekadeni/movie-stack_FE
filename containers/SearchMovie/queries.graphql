query SearchMoviesByKeyword($page: Int, $keyword: String!) {
  searchMoviesByKeyword(page: $page, keyword: $keyword) {
    movies {
      id
      title
      poster
      genres {
        id
        name
      }
      genreIds
      releaseDate
    }
  }
}

query MovieList($page: Int!, $movieType: MovieType!) {
  movieList(page: $page, movieType: $movieType) {
    totalResult
    currentPage
    hasMore
    movies {
      id
      title
      poster
      backdrop
      genreIds
      rating
      summary
      releaseDate
    }
  }
}

query GenreList {
  genreList {
    id
    name
  }
}

query PopularTrailerList {
  popularTrailerList {
    id
    title
    summary
    url
  }
}
